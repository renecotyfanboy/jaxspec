[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "jaxspec"
authors = [{name = "Simon Dupourqué", email = "sdupourque@irap.omp.eu"}]
dynamic = ["version", "description"]
