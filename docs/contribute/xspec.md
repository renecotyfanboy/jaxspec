We like to use XSPEC but it has to stay in a cage so it is not too dangerous.

An image of a docker container with XSPEC configured is available at [dockerhub](https://hub.docker.com/repository/docker/renecoty/heasoft/general).

```bash
docker pull renecoty/heasoft:poetry
```
